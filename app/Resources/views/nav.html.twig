<nav class="navbar navbar-fixed-top navbar-default">
    <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <div class="navbar-brand font_exo_2">Aramis Identity Access Management</div>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            {#{% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}#}
            <ul class="nav navbar-nav">
                {% if is_granted("ROLE_ADMIN") %}
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle font_exo_2" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Candidats <span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <li class="font_exo_2"><a href="{{ path( 'liste_des_candidats' , { 'isArchived' : '0'}) }}"><i class="fa fa-user mini_logo_imag"></i> Accès aux Candidats</a></li>
                        <li class="font_exo_2"><a href="{{ path( 'liste_des_candidats' , { 'isArchived' : '1'}) }}"><i class="fa fa-ban mini_logo_imag"></i> Accès aux Archives</a></li>
                        <li class="font_exo_2"><a href="{{ path( 'liste_des_candidats' , { 'isArchived' : '2'}) }}"><i class="fa fa-ban mini_logo_imag"></i> Accès aux Transformés</a></li>
                    </ul>
                </li>
                {% endif %}
                {% if is_granted("ROLE_SUPER_ADMIN") %}
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle font_exo_2" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Utilisateurs <span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            {% if is_granted("ROLE_SUPER_ADMIN") %}
                                <li class="font_exo_2"><a href="{{ path('liste_des_utilisateurs' , { 'isArchived' : '0'}) }}"><i class="fa fa-users mini_logo_imag"></i> Utilisateurs Actifs</a></li>
                            {% endif %}
                            {% if is_granted("ROLE_SUPER_ADMIN") %}
                                <li class="font_exo_2"><a href="{{ path('liste_des_utilisateurs' , { 'isArchived' : '1'}) }}"><i class="fa fa-user-times mini_logo_imag"></i> Utilisateurs Inactifs</a></li>
                            {% endif %}
                        </ul>
                    </li>
                {% endif %}
                {% if is_granted("ROLE_ADMIN") %}
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle font_exo_2" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Divers <span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            {% if is_granted("ROLE_SUPER_ADMIN") %}
                                <li class="font_exo_2"><a href="{{ path('liste_des_entites_holding') }}"><i class="fa fa-home mini_logo_imag"></i> Liste des Entités</a></li>
                            {% endif %}
                            {% if is_granted("ROLE_SUPER_ADMIN") %}
                                <li class="font_exo_2"><a href="{{ path('liste_des_agences') }}"><i class="fa fa-home mini_logo_imag"></i> Liste des Agences</a></li>
                            {% endif %}
                            {% if is_granted("ROLE_SUPER_ADMIN") %}
                                <li class="font_exo_2"><a href="{{ path('liste_des_services') }}"><i class="fa fa-life-ring mini_logo_imag"></i> Liste des Services</a></li>
                            {% endif %}
                            {% if is_granted("ROLE_ADMIN") %}
                                <li class="font_exo_2"><a href="{{ path('liste_des_fonctions') }}"><i class="fa fa-puzzle-piece mini_logo_imag"></i> Liste des Fonctions</a></li>
                            {% endif %}
                        </ul>
                    </li>
                {% endif %}
                {% if is_granted("ROLE_SUPER_ADMIN") %}
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle font_exo_2" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Odigo <span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            {% if is_granted("ROLE_SUPER_ADMIN") %}
                                <li class="font_exo_2"><a href="{{ path('odigo_tel_liste') }}"><i class="fa fa-phone mini_logo_imag"></i> Numéros Odigo</a></li>
                            {% endif %}
                            {% if is_granted("ROLE_SUPER_ADMIN") %}
                                <li class="font_exo_2"><a href="{{ path('orange_tel_liste') }}"><i class="fa fa-phone mini_logo_imag"></i> Numéros Orange</a></li>
                            {% endif %}
                        </ul>
                    </li>
                {% endif %}
                {% if is_granted("ROLE_SUPER_ADMIN") %}
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle font_exo_2" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Salesforce <span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            {% if is_granted("ROLE_SUPER_ADMIN") %}
                                <li class="font_exo_2"><a href="{{ path('salesforce_profile_liste') }}"><img src="{{ asset('images/salesforce_logo_detail.png') }}" class="mini_logo_image_size"> Profiles Salesforce</a></li>
                            {% endif %}
                            {% if is_granted("ROLE_SUPER_ADMIN") %}
                                <li class="font_exo_2"><a href="{{ path('salesforce_groupe_liste') }}"><img src="{{ asset('images/salesforce_logo_detail.png') }}" class="mini_logo_image_size"> Groupes Salesforce</a></li>
                            {% endif %}
                        </ul>
                    </li>
                {% endif %}
            </ul>
            {#{% endif %}#}
            <ul class="nav navbar-nav navbar-right">
                <li>
                    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                        <a href="{{ path('logout') }}" class="font_exo_2">Déconnexion</a>
                    {% else %}
                        <a href="{{ path('logout') }}" class="font_exo_2">Connexion</a>
                    {% endif %}
                </li>
            </ul>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>

